% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{sci_degradation_curve}
\alias{sci_degradation_curve}
\title{SCI Degradation Curve}
\usage{
sci_degradation_curve(evidence, n_steps = 10L, config = sci_config_default())
}
\arguments{
\item{evidence}{A validated evidence data.frame (from \code{r4subcore}).}

\item{n_steps}{Integer number of degradation steps after the baseline.
Must be at least 1.}

\item{config}{An \code{sci_config} from \code{\link[=sci_config_default]{sci_config_default()}}.}
}
\value{
A tibble with one row per step and columns:
\itemize{
\item \code{step}: integer step index, starting at 0
\item \code{n_degraded}: number of passing checks turned into failures
\item \code{frac_degraded}: \code{n_degraded} divided by the baseline passing count
\item \code{SCI}: the Submission Confidence Index at that step
\item \code{band}: the decision band at that step
\item \code{delta}: change in SCI from the previous step (\code{NA} at step 0)
}
}
\description{
Progressively degrades an evidence table and records how the Submission
Confidence Index responds. The curve is the operational form of construct
validity: a well-behaved index must fall (never rise) as passing checks are
turned into failures.
}
\details{
Starting from the supplied evidence, the function repeatedly converts a
growing number of currently-passing checks to \code{"fail"} and recomputes the
SCI at each step. Passing checks are degraded in order of how much they
contribute to the baseline SCI, largest contribution first, so the curve
shows the steepest descent the evidence allows and is reproducible. Checks
that already \code{warn} or \code{fail} are left untouched.

A passing check contributes \code{1 - severity_weight} to its indicator score, so
ordering by contribution degrades the lower-severity passing checks earliest.
This is a property of the scoring model, not of this function.

Step 0 is the unmodified evidence. The final step degrades every passing
check. When the number of passing checks is smaller than \code{n_steps}, the
number of steps is reduced to the number of passing checks so that each step
degrades at least one additional check.
}
\examples{
\dontrun{
curve <- sci_degradation_curve(evidence, n_steps = 8)
curve
}

}
\seealso{
\code{\link[=sci_monotone_check]{sci_monotone_check()}} to confirm a curve never rises.
}
