% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_scores.R
\name{compute_indicator_scores}
\alias{compute_indicator_scores}
\title{Compute Indicator-Level Scores}
\usage{
compute_indicator_scores(evidence)
}
\arguments{
\item{evidence}{A validated evidence data.frame (from \code{r4subcore}).}
}
\value{
A tibble with columns: \code{indicator_id}, \code{indicator_name},
\code{indicator_domain}, \code{n_evidence}, \code{indicator_score}.
}
\description{
Converts each indicator in an evidence table into a numeric score (0--1)
using severity-weighted result scoring.
}
\details{
For each evidence row:
\itemize{
\item \code{result_score} = \code{r4subcore::result_to_score(result)} (pass=1, warn=0.5, fail=0)
\item \code{severity_weight} = \code{r4subcore::severity_to_weight(severity)} (info=0, ..., critical=1)
\item \code{weighted_score} = \code{result_score * (1 - severity_weight)}
}

Rows are grouped by \code{indicator_id} and \code{indicator_domain}, and the
indicator score is the mean of \code{weighted_score} within each group.
}
\examples{
\dontrun{
scores <- compute_indicator_scores(evidence)
scores
}

}
