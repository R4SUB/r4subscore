% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots.R
\name{sci_snapshot}
\alias{sci_snapshot}
\alias{print.sci_snapshot}
\title{Capture an SCI Snapshot}
\usage{
sci_snapshot(
  evidence,
  label,
  config = sci_config_default(),
  run_context = NULL
)

\method{print}{sci_snapshot}(x, ...)
}
\arguments{
\item{evidence}{A validated evidence data.frame for this run.}

\item{label}{A short label for the run, for example \code{"DB lock minus 8 weeks"}.}

\item{config}{An \code{sci_config} from \code{\link[=sci_config_default]{sci_config_default()}}.}

\item{run_context}{Optional \link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context} to record alongside
the scores.}

\item{x}{An \code{sci_snapshot}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"sci_snapshot"}.
}
\description{
Readiness is a trajectory, not a single moment. A snapshot records the SCI,
the band, and the underlying pillar and indicator scores at one point in time
(a dry run, a database lock) under a label, so later runs can be compared with
\code{\link[=sci_diff]{sci_diff()}} and tracked with \code{\link[=sci_snapshot_history]{sci_snapshot_history()}}.
}
\examples{
ctx <- suppressMessages(r4subcore::r4sub_run_context("S1", "DEV"))
ev <- suppressMessages(r4subcore::as_evidence(
  data.frame(
    asset_type = "dataset", asset_id = "ADSL", source_name = "p21",
    indicator_id = c("Q1", "T1"), indicator_name = c("Q1", "T1"),
    indicator_domain = c("quality", "trace"),
    severity = c("low", "high"), result = c("pass", "fail"),
    stringsAsFactors = FALSE
  ),
  ctx = ctx
))
snap <- sci_snapshot(ev, "dry run 1")
snap

}
\seealso{
\code{\link[=sci_diff]{sci_diff()}}, \code{\link[=sci_snapshot_history]{sci_snapshot_history()}}.
}
