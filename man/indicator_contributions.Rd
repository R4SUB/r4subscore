% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contributions.R
\name{indicator_contributions}
\alias{indicator_contributions}
\title{Per-Indicator Contribution Breakdown}
\usage{
indicator_contributions(evidence, config = sci_config_default())
}
\arguments{
\item{evidence}{A validated evidence data.frame (from \code{r4subcore}).}

\item{config}{An \code{sci_config} from \code{\link[=sci_config_default]{sci_config_default()}}.}
}
\value{
A tibble sorted by \code{sci_headroom} descending, with columns:
\code{indicator_id}, \code{indicator_name}, \code{indicator_domain}, \code{indicator_score},
\code{n_in_pillar}, \code{pillar_contribution}, \code{pillar_headroom}, \code{sci_headroom},
\code{worst_result}, \code{suggestion}.
}
\description{
Breaks a pillar score down into the contribution of each indicator, ranks
indicators by how much score could be recovered by fixing them, and attaches
a short remediation hint for each one. Use this to answer "what do we fix
first?" once \code{\link[=compute_sci]{compute_sci()}} has answered the go/no-go question.
}
\details{
A pillar score is the mean of its indicator scores (missing indicators are
dropped), so within a pillar with \code{k} scored indicators each indicator
contributes \code{indicator_score / k} to the pillar score. Bringing one indicator
up to a perfect 1 would raise the pillar score by \code{(1 - indicator_score) / k};
that quantity is the \code{pillar_headroom}. Multiplying it by the pillar weight
gives \code{sci_headroom}, an approximate cap on how many SCI points a single fix
can buy. Indicators are returned in \code{sci_headroom} order so the highest-value
fixes come first.

The \code{sci_headroom} figure uses the nominal pillar weight from \code{config}. When
a whole pillar is missing its weight is redistributed across the remaining
pillars, so the exact SCI movement from a fix can differ slightly. Use
\code{\link[=sci_what_if]{sci_what_if()}} for an exact recomputation.
}
\examples{
\dontrun{
contrib <- indicator_contributions(evidence)
head(contrib)
}

}
