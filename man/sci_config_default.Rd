% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{sci_config_default}
\alias{sci_config_default}
\title{Default SCI Configuration}
\usage{
sci_config_default(
  pillar_weights = c(quality = 0.35, trace = 0.25, risk = 0.25, usability = 0.15),
  bands = list(ready = c(85, 100), minor_gaps = c(70, 84), conditional = c(50, 69),
    high_risk = c(0, 49))
)
}
\arguments{
\item{pillar_weights}{Named numeric vector of weights for each pillar.
Must sum to 1. Names must be a subset of
\code{"quality"}, \code{"trace"}, \code{"risk"}, \code{"usability"}.}

\item{bands}{Named list of numeric length-2 vectors defining SCI band
boundaries \code{c(lower, upper)}. Evaluated in order; first match wins.}
}
\value{
A list of class \code{"sci_config"} with elements:
\code{pillar_weights}, \code{bands}.
}
\description{
Returns a configuration list with default pillar weights, decision bands,
and scoring parameters for the Submission Confidence Index.
}
\examples{
cfg <- sci_config_default()
cfg$pillar_weights
cfg$bands

# Custom weights (must sum to 1)
sci_config_default(
  pillar_weights = c(quality = 0.40, trace = 0.20, risk = 0.30, usability = 0.10)
)

}
