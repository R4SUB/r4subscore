% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{sci_gap_to_target}
\alias{sci_gap_to_target}
\title{Measure the Gap Between a Scored Run and a Target}
\usage{
sci_gap_to_target(x, targets = "ready", config = sci_config_default())
}
\arguments{
\item{x}{An \code{sci_result} from \code{\link[=compute_sci]{compute_sci()}}.}

\item{targets}{An \code{sci_targets} object, or a band name / numeric passed
straight to \code{\link[=sci_targets]{sci_targets()}}.}

\item{config}{An \code{sci_config}, used only when \code{targets} needs building.}
}
\value{
A list of class \code{"sci_gap"} with \code{current_sci}, \code{target_sci}, \code{gap}
(0 when met, otherwise the shortfall), \code{met} (logical), \code{band},
\code{target_band}, and \code{pillars}: a data.frame with \code{pillar}, \code{weight},
\code{current}, \code{target}, \code{gap}, \code{met}, \code{max_lift}, and \code{target_lift}.
}
\description{
Compares a computed SCI result against an \code{\link[=sci_targets]{sci_targets()}} goal and reports
the shortfall overall and per pillar. For each pillar it also reports the
SCI points that closing that pillar would add, so a team can see which pillar
is the biggest lever rather than guessing.
}
\details{
Because the SCI is a weighted average, each pillar contributes
\code{weight * pillar_score} points to the 0--100 total. The headroom a pillar
offers is therefore \code{weight * (100 - current)}, reported as \code{max_lift}, and
the pillars are ordered by it so the largest opportunity is first.
}
\examples{
\dontrun{
ps <- compute_pillar_scores(evidence)
res <- compute_sci(ps)
sci_gap_to_target(res, "ready")
}

}
