% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets.R
\name{sci_targets}
\alias{sci_targets}
\title{Define an SCI Target}
\usage{
sci_targets(
  target = "ready",
  pillar_targets = NULL,
  config = sci_config_default()
)
}
\arguments{
\item{target}{Either a band name (for example \code{"ready"}), in which case the
target SCI is that band's lower bound, or a single numeric SCI on the
0--100 scale.}

\item{pillar_targets}{Optional named numeric vector of per-pillar minimum
scores on the 0--100 scale. Names must be among \code{"quality"}, \code{"trace"},
\code{"risk"}, \code{"usability"}.}

\item{config}{An \code{sci_config} from \code{\link[=sci_config_default]{sci_config_default()}}, used to resolve a
band name to a numeric bound and to classify a numeric target.}
}
\value{
A list of class \code{"sci_targets"} with \code{sci} (numeric target),
\code{band} (the band the target falls in), and \code{pillars} (named numeric vector
of per-pillar targets, possibly empty).
}
\description{
Records the readiness goal a submission is aiming for: an overall SCI target
and, optionally, a minimum score for individual pillars. Pair it with
\code{\link[=sci_gap_to_target]{sci_gap_to_target()}} to measure how far a scored run is from filing.
}
\examples{
sci_targets("ready")
sci_targets(80, pillar_targets = c(risk = 75, trace = 85))

}
